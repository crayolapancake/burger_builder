// Auxiliary Higher Order Component (wrapper)
// functional component without state or hooks.
// no JSX so don't hve to import react.

const auxHOC = (props) => props.children;


export default auxHOC
